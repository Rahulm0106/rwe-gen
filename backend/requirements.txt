# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0

# ── Data validation ───────────────────────────────────────────────────────────
pydantic==2.7.1
pydantic-settings==2.2.1

# ── HTTP client — used by Simon's llm_module and concept_mapping_module ───────
# Both modules use the requests library (not httpx) for external API calls.
# Venice API + LOF/IMO token endpoint + IMO normalize endpoint all use requests.
requests==2.31.0

# ── JSON schema validation — used by Simon's LLM and concept mapping modules ──
# Draft202012Validator validates LLM output against protocol_schema_validator.json
jsonschema==4.22.0

# ── Database ──────────────────────────────────────────────────────────────────
psycopg2-binary==2.9.9

# ── Environment variables ─────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0       # used by FastAPI TestClient only — NOT for LLM API calls

# ── Code quality ──────────────────────────────────────────────────────────────
ruff==0.4.4

# ── REMOVED vs Sprint 1 ───────────────────────────────────────────────────────
# rapidfuzz   — Simon's local matcher uses rule-based scoring, not rapidfuzz
# groq        — replaced by Venice (requests-based, no SDK needed)
